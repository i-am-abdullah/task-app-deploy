export enum ProjectStatus {
  ACTIVE = 'active',
  ARCHIVED = 'archived'
}

export enum BoardStatus {
  ACTIVE = 'active',
  ARCHIVED = 'archived'
}

export enum UserRole {
  ADMIN = 'admin',
  TEAM_LEAD = 'team_lead',
  USER = 'user'
}